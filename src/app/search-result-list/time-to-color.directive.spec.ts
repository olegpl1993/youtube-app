import { TimeToColorDirective } from './time-to-color.directive';

describe('TimeToColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeToColorDirective();
    expect(directive).toBeTruthy();
  });
});
